type Props = {
  showAddMedia: React.Dispatch<React.SetStateAction<boolean>>;
};
const AddStatusMedia = ({ showAddMedia }: Props) => {
  return <div>AddStatusMedia</div>;
};

export default AddStatusMedia;
